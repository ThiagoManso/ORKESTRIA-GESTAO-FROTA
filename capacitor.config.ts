import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orkestria.app',
  appName: 'Orkestria Control Tower',
  webDir: 'dist'
};

export default config;
